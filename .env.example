# DockSlot Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================================================
# Required - Supabase
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================================================
# Optional - Email Notifications (Resend)
# ============================================================================
# Required for booking confirmations, weather alerts, review requests
# Get your API key from https://resend.com/api-keys
# Free tier: 3,000 emails/month
RESEND_API_KEY=
# From address for outbound emails. Defaults to onboarding@resend.dev (Resend sandbox).
# Once you verify your domain in Resend, set this to bookings@yourdomain.com
RESEND_FROM_EMAIL=

# ============================================================================
# Optional - Payment Processing (Stripe)
# ============================================================================
# Required for accepting deposits and balance payments
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Required for Captain Pro subscriptions ($29/month)
# Create a Product + Price in Stripe Dashboard, then copy the Price ID
# https://dashboard.stripe.com/products → Add product → $29/month recurring
STRIPE_PRO_PRICE_ID=

# ============================================================================
# Optional - Cron Job Security
# ============================================================================
# Used to secure cron endpoints (weather checks, review reminders, etc.)
# Generate a random secret: `openssl rand -base64 32`
CRON_SECRET=

# ============================================================================
# Optional - Application URL
# ============================================================================
# Used for generating absolute URLs in emails
# Defaults to request origin if not set
NEXT_PUBLIC_APP_URL=https://your-domain.com

# ============================================================================
# Optional - Vercel Deployment (auto-configured in Vercel)
# ============================================================================
VERCEL_TOKEN=
VERCEL_OIDC_TOKEN=
